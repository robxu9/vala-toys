All:

* autohellify! [DONE]

Symbol Completion:

* type inference in 'var' expressions [DONE]
* type inference in lambda expressions [DONE]
* unify datatype inference and datatype descriptors routines

Bracker Completion:
* support for [] as an array declarator or as an address to an array
   element
* support for { }; in lamba expressions
